# <プロジェクト名> — Claude Code 設定 (L3)

グローバル設定 (`~/CLAUDE.md`・`~/RULES.md`、agent-rules から symlink) を継承する。ここには**このプロジェクト固有**の情報だけを書く (グローバルに書いてある一般規約・スキルの使い方は繰り返さない)。横断で再利用しそうな内容に気づいたら `agent-rules/docs/notes/promotion-candidates.md` に候補記録 (2 回目ルール)。

## 概要

(何のプロジェクトか 1-2 行)

## ビルド・テスト・実行

- build:
- test:
- run:

## このプロジェクト固有の規約・gotcha

-

## 参照

- 設計・決定: `docs/design/` / `docs/adr/`
- (Linear があれば) Project/Issue へのリンク
