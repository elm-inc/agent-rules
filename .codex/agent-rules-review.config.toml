# Review-oriented Codex config layer.
#
# Use with:
#   codex --profile agent-rules-review review --uncommitted
#   codex exec --profile agent-rules-review "レビューして"

model_reasoning_effort = "high"
model_verbosity = "medium"
