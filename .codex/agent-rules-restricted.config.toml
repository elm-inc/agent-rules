# Restricted Codex config layer for audits and read-only inspection.
#
# Use with:
#   codex --profile agent-rules-restricted
#   codex exec --profile agent-rules-restricted "調査して"

sandbox_mode = "read-only"
model_reasoning_effort = "medium"
