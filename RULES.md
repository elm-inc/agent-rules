# 共通ルール

このファイルは Claude Code / Codex CLI 共通のルール定義である。
各ツールの定義ファイル（`CLAUDE.md`, `AGENTS.md`）から参照される。

## 言語・コミュニケーション
- 日本語で応答する
- 技術用語・変数名・関数名・コマンドは英語のまま
- 曖昧な指示は実行前に確認を取る。推測で進めない

## コーディング原則
- 既存のコードスタイル・パターン・規約をまず把握する
- プロジェクトのリンター・フォーマッター設定を尊重し、上書き・変更しない
- 依存パッケージ・ライブラリの追加は事前に確認を取る
- Makefile やスクリプトにタスクが定義されている場合はそれを使う
- リファクタリングや改善を行う場合は、既存のコード・規約を把握した上で改善案を提示し、承認を得てから実施する
- 要求された範囲外のコードを無断で変更しない
- 新規ファイル作成より既存ファイルの編集を優先する

## Git ルール
- コミットメッセージは Conventional Commits 形式（`feat:`, `fix:`, `refactor:`, `perf:`, `chore:`, `test:`, `docs:`）
- コミットメッセージは日本語で書く（例: `feat: ユーザー認証を追加`）
- `git push` は明示的な指示があるまで実行しない
- `git push --force`, `git reset --hard`, `git clean -f` は明示的な許可なく実行しない
- `main`, `master`, `develop` 等の保護ブランチへの直接操作は禁止
- 大きな変更は意味のある単位でコミットを分割する
- **コミット前に必ず Codex CLI によるレビューを実施する**。レビュー結果に重大な指摘がある場合はコミット前に修正する

### コミット時のフロー
1. コード変更・テスト完了
2. Codex CLI でレビューを実施（uncommitted な変更が対象）
3. レビュー結果を確認し、重大な指摘があれば修正
4. 問題なければコミットを実行

## セキュリティ
- `.env`, 認証情報, 秘密鍵, トークンをコミット・表示・ログ出力しない
- シークレットをコード内にハードコードしない
- 認証情報が必要な操作は手順のみ示し、値の入力はユーザに委ねる

## 禁止事項（明示的な許可なく実行しない）
- ファイル・ディレクトリの再帰的削除（`rm -rf` 等）
- コンテナ・ボリュームの破壊的操作（`docker system prune`, `docker volume rm` 等）
- 本番・ステージング環境への操作（デプロイ、DB変更、インフラ変更）
- プロジェクトの設定ファイル（ビルド設定、CI/CD設定、Docker設定等）の変更
- ユーザのグローバル設定（シェル設定、Git設定、SSH設定等）の変更
- パッケージマネージャが管理するディレクトリ（`node_modules/`, `vendor/` 等）の直接編集

## テスト
- コード変更後は関連するテストを実行して動作を確認する
- テストが失敗した状態で「完了」と報告しない
- 新機能にはテストコードも書く

## ファイル管理
- 不要なファイル（メモ、一時ファイル、バックアップ、README等）を勝手に作成しない
- ファイル作成が必要な場合はプロジェクトの既存ディレクトリ構造に従う
