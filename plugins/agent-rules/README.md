# agent-rules plugin

This is a minimal Codex plugin scaffold for publishing the shared `agent-rules` baseline through a marketplace when symlink-only installation is not enough.

The primary installation path remains:

```bash
./install.sh
```

The plugin scaffold is intentionally small. The canonical rules, scripts, MCP fragments, and skills remain in the repository root.
